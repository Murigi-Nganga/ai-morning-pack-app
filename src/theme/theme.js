// Theme to be used in the entire application
import { createTheme } from "@material-ui/core";

export const appTheme = createTheme({
    typography: {
        fontFamily: 'Quicksand',
        color: 'black',
        h4: {
            fontFamily: 'Merienda',
            fontWeight: 200,
        }
    }
})